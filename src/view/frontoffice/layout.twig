<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Job Dating - YouCode{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .line-clamp-3 {
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        /* Animations */
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .animate-spin {
            animation: spin 1s linear infinite;
        }

        /* Gradient backgrounds */
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .gradient-bg-blue {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        }

        .gradient-bg-purple {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .gradient-bg-pink {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        }

        /* Custom scrollbar */
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 10px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: #c1c1c1;
            border-radius: 10px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: #a1a1a1;
        }

        /* Dark mode support */
        .dark .bg-gray-50 {
            background-color: #111827;
        }

        .dark .bg-white {
            background-color: #1f2937;
        }

        .dark .text-gray-900 {
            color: #f9fafb;
        }

        .dark .text-gray-600 {
            color: #d1d5db;
        }

        .dark .border-gray-300 {
            border-color: #374151;
        }

        /* Glass morphism effect */
        .glass-effect {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .dark .glass-effect {
            background: rgba(31, 41, 55, 0.7);
            border: 1px solid rgba(75, 85, 99, 0.2);
        }

        /* Additional styles for layout */
        .nav-link {
            @apply text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 font-medium transition-colors duration-200 px-3 py-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700;
        }

        .mobile-nav-link {
            @apply block text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 font-medium transition-colors duration-200 px-4 py-3 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700;
        }

        .dropdown-item {
            @apply block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors duration-150;
        }

        /* Animation for flash messages */
        @keyframes slideIn {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .animate-slide-in {
            animation: slideIn 0.3s ease-out;
        }

        /* Smooth transitions */
        .transition-all {
            transition-property: all;
            transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
            transition-duration: 200ms;
        }
    </style>
    {% block styles %}{% endblock %}
</head>
<body class="bg-gray-50 dark:bg-gray-900 transition-colors duration-200">
<!-- Navigation -->
<nav class="bg-white dark:bg-gray-800 shadow-lg border-b border-gray-200 dark:border-gray-700 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo / Brand -->
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 rounded-lg gradient-bg flex items-center justify-center">
                    <i class="fas fa-briefcase text-white"></i>
                </div>
                <div>
                    <h1 class="text-xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                        YouCode Job Dating
                    </h1>
                    <p class="text-xs text-gray-500 dark:text-gray-400">Opportunités professionnelles</p>
                </div>
            </div>

            <!-- Search Bar -->
            <div class="hidden md:block w-full md:w-auto md:flex-1 md:max-w-xl mx-0 md:mx-4">
                <div class="relative">
                    <input type="text" id="jobSearch"
                           placeholder="Search jobs, companies, keywords..."
                           class="w-full pl-12 pr-4 py-2.5 rounded-full border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-transparent">
                    <span class="absolute left-4 top-2.5 text-gray-400">🔍</span>
                </div>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-4">
                <!-- Navigation Links -->
                <div class="flex items-center space-x-2">
                    <a href="/home" class="nav-link {% if current_page == 'home' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                        <i class="fas fa-home mr-2"></i>
                        Accueil
                    </a>
                    <a href="/offres" class="nav-link {% if current_page == 'offres' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                        <i class="fas fa-search mr-2"></i>
                        Offres d'emploi
                    </a>
                    <a href="/entreprises" class="nav-link {% if current_page == 'entreprises' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                        <i class="fas fa-building mr-2"></i>
                        Entreprises
                    </a>
                </div>

                <!-- Filters Button -->
                <div class="relative">
                    <button id="filtersToggle" class="flex items-center gap-2 px-4 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium">
                        <span>📂 Filters</span>
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                    </button>

                    <!-- Filters Dropdown Panel -->
                    <div id="filtersPanel" class="hidden absolute right-0 mt-2 w-80 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 p-6 z-50">
                        <div class="flex justify-between items-center mb-4">
                            <h3 class="font-bold text-gray-800 dark:text-white text-lg">Filter Jobs</h3>
                            <button type="button" onclick="closeFilters()" class="text-gray-400 hover:text-gray-600 dark:hover:text-gray-300">
                                ✕
                            </button>
                        </div>

                        <form id="filterForm" class="space-y-6">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Company</label>
                                <select name="company" class="w-full border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-lg text-sm focus:ring-blue-500 focus:border-blue-500">
                                    <option value="">All Companies</option>
                                    {% for company in companies|default([]) %}
                                        <option value="{{ company.id }}">{{ company.name }}</option>
                                    {% endfor %}
                                </select>
                            </div>

                            <div>
                                <label class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Contract Type</label>
                                <div class="grid grid-cols-2 gap-2">
                                    {% for type in ['CDI', 'CDD', 'Internship', 'Freelance'] %}
                                        <label class="flex items-center text-sm text-gray-600 dark:text-gray-400 hover:bg-gray-50 dark:hover:bg-gray-700 p-2 rounded">
                                            <input type="checkbox" name="contract[]" value="{{ type }}" class="rounded text-blue-600 mr-2">
                                            {{ type }}
                                        </label>
                                    {% endfor %}
                                </div>
                            </div>

                            <div class="flex justify-between pt-4 border-t border-gray-100 dark:border-gray-700">
                                <button type="reset" class="text-sm text-gray-600 dark:text-gray-400 hover:text-gray-800 dark:hover:text-gray-300 font-medium">
                                    Clear all
                                </button>
                                <button type="button" onclick="closeFilters()" class="px-4 py-2 bg-blue-600 text-white rounded-lg text-sm font-medium hover:bg-blue-700">
                                    Apply Filters
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- User Dropdown -->
                <div class="relative group">
                    <button class="flex items-center space-x-2 focus:outline-none">
                        <div class="w-8 h-8 rounded-full bg-gradient-to-r from-blue-500 to-purple-500 flex items-center justify-center text-white font-bold">
                            {{ user_initials|default('U') }}
                        </div>
                        <i class="fas fa-chevron-down text-gray-500 text-sm"></i>
                    </button>

                    <div class="absolute right-0 mt-2 w-48 bg-white dark:bg-gray-800 rounded-lg shadow-xl border border-gray-200 dark:border-gray-700 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-10">
                        <div class="py-2">
                            <div class="px-4 py-2 border-b border-gray-100 dark:border-gray-700">
                                <p class="text-sm font-semibold text-gray-900 dark:text-white">{{ user_name|default('Utilisateur') }}</p>
                                <p class="text-xs text-gray-500 dark:text-gray-400">{{ user_email|default('user@example.com') }}</p>
                            </div>
                            <a href="/profile" class="dropdown-item">
                                <i class="fas fa-user mr-3"></i>
                                Mon Profil
                            </a>
                            <a href="/applications" class="dropdown-item">
                                <i class="fas fa-file-alt mr-3"></i>
                                Mes Candidatures
                            </a>
                            <a href="/saved-jobs" class="dropdown-item">
                                <i class="fas fa-bookmark mr-3"></i>
                                Offres Sauvegardées
                            </a>
                            <div class="border-t border-gray-100 dark:border-gray-700 mt-2 pt-2">
                                <a href="/logout" class="dropdown-item text-red-600 hover:text-red-700 dark:text-red-400">
                                    <i class="fas fa-sign-out-alt mr-3"></i>
                                    Déconnexion
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden flex items-center space-x-4">
                <!-- Mobile Search Button -->
                <button id="mobileSearchButton" class="text-gray-500 hover:text-gray-700 dark:hover:text-gray-300">
                    <i class="fas fa-search text-xl"></i>
                </button>

                <button id="mobileMenuButton" class="text-gray-500 hover:text-gray-700 dark:hover:text-gray-300 focus:outline-none">
                    <i class="fas fa-bars text-xl"></i>
                </button>
            </div>
        </div>

        <!-- Mobile Search Bar -->
        <div id="mobileSearch" class="md:hidden hidden py-4 border-t border-gray-200 dark:border-gray-700">
            <div class="relative">
                <input type="text" id="mobileJobSearch"
                       placeholder="Search jobs, companies, keywords..."
                       class="w-full pl-12 pr-4 py-3 rounded-full border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-transparent">
                <span class="absolute left-4 top-3.5 text-gray-400">🔍</span>
            </div>
        </div>

        <!-- Mobile menu -->
        <div id="mobileMenu" class="md:hidden hidden py-4 border-t border-gray-200 dark:border-gray-700">
            <div class="space-y-3">
                <a href="/home" class="mobile-nav-link {% if current_page == 'home' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                    <i class="fas fa-home mr-3"></i>
                    Accueil
                </a>
                <a href="/offres" class="mobile-nav-link {% if current_page == 'offres' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                    <i class="fas fa-search mr-3"></i>
                    Offres d'emploi
                </a>
                <a href="/entreprises" class="mobile-nav-link {% if current_page == 'entreprises' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                    <i class="fas fa-building mr-3"></i>
                    Entreprises
                </a>
                <a href="/dashboard" class="mobile-nav-link {% if current_page == 'dashboard' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                    <i class="fas fa-tachometer-alt mr-3"></i>
                    Dashboard
                </a>

                <!-- Mobile Filters -->
                <div class="px-4 py-3">
                    <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Filters</h4>
                    <div class="space-y-4">
                        <div>
                            <label class="block text-xs font-semibold text-gray-600 dark:text-gray-400 mb-1">Company</label>
                            <select name="company" class="w-full border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-lg text-sm p-2">
                                <option value="">All Companies</option>
                                {% for company in companies|default([]) %}
                                    <option value="{{ company.id }}">{{ company.name }}</option>
                                {% endfor %}
                            </select>
                        </div>

                        <div>
                            <label class="block text-xs font-semibold text-gray-600 dark:text-gray-400 mb-1">Contract Type</label>
                            <div class="flex flex-wrap gap-2">
                                {% for type in ['CDI', 'CDD', 'Internship', 'Freelance'] %}
                                    <label class="flex items-center text-xs text-gray-600 dark:text-gray-400">
                                        <input type="checkbox" name="contract[]" value="{{ type }}" class="rounded text-blue-600 mr-1">
                                        {{ type }}
                                    </label>
                                {% endfor %}
                            </div>
                        </div>

                        <button type="reset" class="text-xs text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 font-medium">
                            Clear all filters
                        </button>
                    </div>
                </div>

                <div class="pt-3 border-t border-gray-200 dark:border-gray-700">
                    <a href="/profile" class="mobile-nav-link {% if current_page == 'profile' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                        <i class="fas fa-user mr-3"></i>
                        Mon Profil
                    </a>
                    <a href="/applications" class="mobile-nav-link {% if current_page == 'applications' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                        <i class="fas fa-file-alt mr-3"></i>
                        Mes Candidatures
                    </a>
                    <a href="/saved-jobs" class="mobile-nav-link {% if current_page == 'saved-jobs' %}bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400{% endif %}">
                        <i class="fas fa-bookmark mr-3"></i>
                        Offres Sauvegardées
                    </a>
                    <a href="/logout" class="mobile-nav-link text-red-600 hover:text-red-700 dark:text-red-400">
                        <i class="fas fa-sign-out-alt mr-3"></i>
                        Déconnexion
                    </a>
                </div>
            </div>
        </div>
    </div>
</nav>

<!-- Main Content -->
<main>
    {% block content %}{% endblock %}
</main>

<!-- Footer -->
<footer class="bg-gray-800 text-white mt-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div>
                <div class="flex items-center space-x-3 mb-4">
                    <div class="w-10 h-10 rounded-lg gradient-bg flex items-center justify-center">
                        <i class="fas fa-briefcase text-white"></i>
                    </div>
                    <h2 class="text-xl font-bold">YouCode Job Dating</h2>
                </div>
                <p class="text-gray-400">
                    Plateforme de mise en relation entre les apprenants YouCode et les entreprises partenaires.
                </p>
            </div>

            <div>
                <h3 class="text-lg font-bold mb-4">Liens rapides</h3>
                <ul class="space-y-2">
                    <li><a href="/offres" class="text-gray-400 hover:text-white transition">Offres d'emploi</a></li>
                    <li><a href="/entreprises" class="text-gray-400 hover:text-white transition">Entreprises</a></li>
                    <li><a href="/about" class="text-gray-400 hover:text-white transition">À propos</a></li>
                    <li><a href="/contact" class="text-gray-400 hover:text-white transition">Contact</a></li>
                </ul>
            </div>

            <div>
                <h3 class="text-lg font-bold mb-4">Contact</h3>
                <ul class="space-y-2 text-gray-400">
                    <li class="flex items-center">
                        <i class="fas fa-map-marker-alt mr-3"></i>
                        YouCode, Safi, Maroc
                    </li>
                    <li class="flex items-center">
                        <i class="fas fa-envelope mr-3"></i>
                        contact@youcode.ma
                    </li>
                    <li class="flex items-center">
                        <i class="fas fa-phone mr-3"></i>
                        +212 5XX XX XX XX
                    </li>
                </ul>
            </div>
        </div>

        <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
            <p>&copy; {{ "now"|date("Y") }} YouCode Job Dating. Tous droits réservés.</p>
        </div>
    </div>
</footer>

<!-- Flash Messages Container -->
<div id="flashMessages" class="fixed top-20 right-4 z-50 space-y-2 max-w-sm"></div>

<!-- Scripts -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    // Mobile menu toggle
    $('#mobileMenuButton').on('click', function() {
        $('#mobileMenu').slideToggle(200);
    });

    // Mobile search toggle
    $('#mobileSearchButton').on('click', function() {
        $('#mobileSearch').slideToggle(200);
    });

    // Close mobile menu when clicking outside
    $(document).on('click', function(e) {
        if (!$(e.target).closest('#mobileMenu, #mobileMenuButton, #mobileSearch, #mobileSearchButton').length) {
            $('#mobileMenu').slideUp(200);
            $('#mobileSearch').slideUp(200);
        }
    });

    // Toggle filters panel
    $('#filtersToggle').on('click', function(e) {
        e.stopPropagation();
        $('#filtersPanel').toggleClass('hidden');
    });

    // Close filters when clicking outside
    $(document).on('click', function(e) {
        if (!$(e.target).closest('#filtersPanel, #filtersToggle').length) {
            $('#filtersPanel').addClass('hidden');
        }
    });

    // Close filters function
    function closeFilters() {
        $('#filtersPanel').addClass('hidden');
    }

    // Search functionality
    function performSearch() {
        const query = $('#jobSearch').val() || $('#mobileJobSearch').val() || '';
        const formData = $('#filterForm').serialize();

        // AJAX search would go here
        console.log('Searching for:', query);
        console.log('Filters:', formData);

        // This would trigger your AJAX search
        // $.ajax({
        //     url: '/jobs/search',
        //     method: 'GET',
        //     data: formData + '&q=' + query,
        //     dataType: 'json',
        //     success: function(jobs) {
        //         // Update job listings
        //     }
        // });
    }

    // Debounced search
    let searchTimeout;
    $('#jobSearch, #mobileJobSearch, #filterForm input, #filterForm select').on('keyup change', function() {
        clearTimeout(searchTimeout);
        searchTimeout = setTimeout(performSearch, 300);
    });

    // Flash messages handling
    function showFlashMessage(type, message) {
        const colors = {
            success: 'bg-green-500 border-green-600',
            error: 'bg-red-500 border-red-600',
            warning: 'bg-yellow-500 border-yellow-600',
            info: 'bg-blue-500 border-blue-600'
        };

        const icon = {
            success: 'check-circle',
            error: 'exclamation-circle',
            warning: 'exclamation-triangle',
            info: 'info-circle'
        };

        const flashHtml = `
            <div class="flash-message ${colors[type]} text-white rounded-lg shadow-lg p-4 border-l-4 flex items-center space-x-3 animate-slide-in">
                <i class="fas fa-${icon[type]} text-lg"></i>
                <span class="flex-1">${message}</span>
                <button class="close-flash text-white hover:text-gray-200">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        `;

        $('#flashMessages').append(flashHtml);

        // Auto-remove after 5 seconds
        setTimeout(() => {
            $(`.flash-message`).first().animate({opacity: 0, right: '-100%'}, 500, function() {
                $(this).remove();
            });
        }, 5000);
    }

    // Close flash message
    $(document).on('click', '.close-flash', function() {
        $(this).closest('.flash-message').animate({opacity: 0, right: '-100%'}, 500, function() {
            $(this).remove();
        });
    });

    // Initialize dark mode if saved
    if (localStorage.getItem('darkMode') === 'true') {
        document.documentElement.classList.add('dark');
    }

    // Show any server-side flash messages
    {% if flash_messages is defined %}
    {% for flash in flash_messages %}
    showFlashMessage('{{ flash.type }}', '{{ flash.message }}');
    {% endfor %}
    {% endif %}
</script>

{% block scripts %}{% endblock %}
</body>
</html>